# PostgreSQL local (docker compose up -d). Se usa desde la fase 4.
DATABASE_URL=postgres://volantines:volantines@localhost:5433/volantines
OPENAI_API_KEY=

# Token para GET /api/admin/stats (Authorization: Bearer <token>), mínimo 16 caracteres. Sin él, la ruta no existe.
ADMIN_TOKEN=
